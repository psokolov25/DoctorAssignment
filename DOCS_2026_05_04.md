# DOCS 2026-05-04 — JavaDoc и документация доменного контура

## Что улучшено

1. Расширен JavaDoc у `DoctorServiceSelectionService`:
   - добавлено описание роли контракта в pipeline назначения;
   - зафиксированы сценарии: локальный выбор, med-robot, fallback;
   - документированы входные параметры и поведение `Optional.empty()`.

2. Расширен JavaDoc у `VisitAssignmentExecutor`:
   - уточнено разделение этапов selection/mutation;
   - перечислены цели выделения интерфейса (тестируемость, dry-run, post-check);
   - детально описаны параметры `assign(...)`.

3. В `README.md` добавлен раздел `2.3 JavaDoc-покрытие доменного контура`:
   - перечислены ключевые классы/интерфейсы, где поддерживается углубленный JavaDoc;
   - описана практическая польза для сопровождения и онбординга.

## Зачем это важно

- Уменьшается время восстановления контекста для разработчика при работе с обработкой визитов.
- Доменные границы (выбор услуги vs выполнение мутаций) становятся явными прямо из интерфейсов.
- Снижается риск неверной интерпретации поведения `transfer-only` и fallback-веток.
